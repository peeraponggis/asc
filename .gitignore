bin/master_shellOLD.html

# ── Capacitor / Android ────────────────────────────────────────────────
# node_modules ติดตั้งใหม่ได้ด้วย npm install จึงไม่ต้องเก็บ (27 MB)
capacitor/node_modules/

# ผลลัพธ์การ build และไฟล์ชั่วคราวของ Gradle
capacitor/android/build/
capacitor/android/app/build/
capacitor/android/.gradle/
capacitor/android/local.properties
capacitor/android/.idea/
capacitor/android/app/release/
capacitor/android/app/debug/
*.apk
*.aab
*.keystore
*.jks

# ไฟล์เว็บที่ Capacitor คัดลอกเข้ามา — ต้นฉบับอยู่ที่ docs/
capacitor/android/app/src/main/assets/public/

# ── Supabase ───────────────────────────────────────────────────────────
# ไลบรารีสำหรับสคริปต์นำเข้าผู้ใช้ ติดตั้งใหม่ได้ด้วย npm install
supabase/node_modules/
supabase/package-lock.json

# ── Supabase CLI ───────────────────────────────────────────────────────
# ไฟล์ชั่วคราวของคำสั่ง supabase link/start เก็บรหัสและ ref ของโปรเจกต์
supabase/.temp/
supabase/.branches/
# ไฟล์ตัวแปรลับ เช่น สตริงต่อฐานข้อมูลและคีย์ service_role
.env
.env.*

# ── Claude Code ────────────────────────────────────────────────────────
# worktree และไฟล์ทำงานชั่วคราวของผู้ช่วย AI
.claude/

# ทางลัดของ Windows ที่ชี้มายังโฟลเดอร์นี้เอง เป็นไฟล์ส่วนตัวของเครื่อง
*.lnk
