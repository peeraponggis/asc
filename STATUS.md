# สถานะงาน ASC Cloud

ไฟล์นี้คือ **แหล่งความจริงเดียว** ของงานย้าย ASC ขึ้น Supabase
ทุกเซสชันที่ทำงานกับ repo นี้ให้อ่านไฟล์นี้ก่อน และอัปเดตทุกครั้งที่มีอะไรเปลี่ยน

อัปเดตล่าสุด : 20 ส.ค. 2569  09:51 น.

---

## กติกาการทำงาน

- **งาน ASC Cloud ทั้งหมดใช้เซสชันเดียว** ห้ามเปิดหลายเซสชันในโฟลเดอร์ `git/asc` พร้อมกัน
  เพราะทุกเซสชันแก้ working tree เดียวกันบนสาขา `main` งานจะทับกันโดยไม่มีใครรู้ตัว
- ถ้าต้องทำงานขนานจริง ให้แยก **worktree** ต่องาน (โฟลเดอร์คนละอัน สาขาคนละอัน)
- **โปรเจกต์ Solar SaaS อยู่คนละ repo** (`14-Pi/Solar SaaS/proinventive-solar`) และคนละบัญชี GitHub
  (`enterprice0945`) ไม่เกี่ยวกับงานนี้ ให้แยกเซสชันไว้ต่างหาก
- แก้ไฟล์ที่รากแล้ว **ต้องรัน `sync-docs.cmd`** ก่อน commit ไม่งั้นเว็บจริงจะยังเป็นของเก่า

---

## ทำเสร็จแล้ว

| หัวข้อ | สถานะ |
|---|---|
| ย้ายฐานข้อมูลขึ้น Supabase + ระบบแชร์โครงการ | ✓ |
| โปรเจกต์จริง `rcaslesvgilknupqmwrp` เชื่อมแล้ว | ✓ |
| `supabase-config.js` บนเว็บจริงชี้โปรเจกต์ใหม่แล้ว | ✓ ตรวจที่ `peeraponggis.github.io/asc` แล้ว |
| ตาราง `profiles` `projects` `project_members` `project_list` + bucket รูป | ✓ |
| ทริกเกอร์ `enforce_invite_only` · `on_auth_user_created` | ✓ ทดสอบแล้วว่ากันการสมัครนอกรายชื่อได้จริงที่ชั้นฐานข้อมูล |
| ตั้งค่า SMTP + Redirect URLs | ✓ ส่งอีเมลได้จริง `HTTP 200` |
| ทดสอบสมัครผ่านฟอร์มจริง ชื่อภาษาไทยเก็บถูกต้อง | ✓ |
| ยังไม่ยืนยันอีเมล → เข้าระบบไม่ได้ | ✓ กันถูกต้อง |
| ปุ่มเปลี่ยนรหัสผ่าน / ชื่อที่แสดง ในหน้า Settings | ✓ |
| การ์ดจัดการรายชื่อผู้มีสิทธิ์ใช้งานในแอป (ไม่ต้องเข้า Dashboard) | ✓ |
| `invite-users.mjs` + ตัวเลือก `--check` `--dry-run` `--only` | ✓ |
| commit ทั้งหมดขึ้น GitHub แล้ว | ✓ ถึง `f461b83` |

---

## ค้างอยู่ — ทำต่อจากตรงนี้

### 1. ส่งคำเชิญ 38 ฉบับ  ← ขั้นถัดไป

รายชื่อทั้งหมด 41 บัญชีใน `user.js` หักผู้ดูแลระบบออก เหลือส่งจริง 38

คีย์ `service_role` เก็บไว้ในไฟล์ `supabase/.env` (gitignore แล้ว) ไม่ต้องตั้งตัวแปรใหม่ทุกครั้ง

```
node supabase/invite-users.mjs --check      # ถามเซิร์ฟเวอร์ว่าใครถูกเชิญไปแล้วบ้าง
node supabase/invite-users.mjs --dry-run    # ดูรายชื่อจาก user.js เฉย ๆ
node supabase/invite-users.mjs              # ส่งจริง ~1 นาทีครึ่ง
```

**ยืนยันแล้วด้วย `--check` เมื่อ 20 ส.ค. 09:51 น. — ยังไม่ได้ส่งคำเชิญเลยสักฉบับ**

| | จำนวน |
|---|---|
| ยังไม่ถูกเชิญ | 38 |
| เชิญแล้ว รอตั้งรหัสผ่าน | 0 |
| ใช้งานแล้ว | 0 |
| บัญชีที่มีอยู่บนเซิร์ฟเวอร์ | 2 (ของเจ้าของระบบ + บัญชีทดสอบ `+asctest`) |

- อีเมลออกไปแล้วเรียกคืนไม่ได้ ตรวจรายชื่อให้แน่ก่อน
- บรรทัดไหนขึ้น ✗ ปล่อยรันจนจบแล้วค่อยแก้ รันซ้ำได้ คนที่ส่งสำเร็จแล้วจะถูกข้ามอัตโนมัติ
- ระวัง `--dry-run` **ไม่ได้ถามเซิร์ฟเวอร์** อ่านรายชื่อจาก `user.js` ในเครื่องอย่างเดียว
  ใช้ `--check` เท่านั้นในการตรวจสถานะจริง

### 2. ตรวจหลังส่ง

- ครบ 38 บัญชี โปรไฟล์ถูกสร้างครบ ไม่มีใครตกหล่น
- ผู้ส่งอีเมลควรเป็น `ProInventive ASC` ไม่ใช่ `noreply@mail.app.supabase.io`
- ลิงก์ในอีเมลต้องพากลับมาที่ `peeraponggis.github.io/asc`

### 3. เก็บกวาด

- ลบบัญชีทดสอบ `+asctest` ที่ Supabase Dashboard → Authentication → Users  **(ยังอยู่ ยืนยันแล้ว 20 ส.ค.)**
- `supabase/package.json` ยังไม่ถูก track ใน git — ตัดสินใจว่าจะ commit หรือใส่ `.gitignore`
- 3 บัญชี (`supet` `lpee` `pat`) ใน `user.js` ยังไม่ใช่อีเมลจริง ถ้าจะให้เข้าระบบต้องเติมอีเมลก่อน

### 4. ให้ผู้ใช้เดิมย้ายข้อมูลขึ้นคลาวด์

แต่ละคนต้องทำเองที่เครื่องของตัวเอง (ข้อมูลเดิมแยกกันอยู่ในเบราว์เซอร์แต่ละเครื่อง)
Settings → การ์ด *ย้ายโครงการเดิมขึ้นคลาวด์* → **ส่งขึ้นคลาวด์ทั้งหมด** — กดครั้งเดียวต่อเครื่อง

---

## ข้อควรระวังที่ยังอยู่

- ไม่มีการล็อกเมื่อแก้พร้อมกัน คนบันทึกทีหลังทับคนแรก
- ต้องออนไลน์เสมอ ไม่มีโหมดออฟไลน์
- ถอดชื่อออกจากรายการ = กันสมัครใหม่เท่านั้น ไม่ได้ลบบัญชีที่มีอยู่แล้ว

รายละเอียดการติดตั้งทั้งหมดอยู่ใน [`supabase/README.md`](supabase/README.md)
