# สถานะงาน ASC Cloud

ไฟล์นี้คือ **แหล่งความจริงเดียว** ของงานย้าย ASC ขึ้น Supabase
ทุกเซสชันที่ทำงานกับ repo นี้ให้อ่านไฟล์นี้ก่อน และอัปเดตทุกครั้งที่มีอะไรเปลี่ยน

อัปเดตล่าสุด : 20 ส.ค. 2569

---

## กติกาการทำงาน

- **งาน ASC Cloud ทั้งหมดใช้เซสชันเดียว** ห้ามเปิดหลายเซสชันในโฟลเดอร์ `git/asc` พร้อมกัน
  เพราะทุกเซสชันแก้ working tree เดียวกันบนสาขา `main` งานจะทับกันโดยไม่มีใครรู้ตัว
- ถ้าต้องทำงานขนานจริง ให้แยก **worktree** ต่องาน (โฟลเดอร์คนละอัน สาขาคนละอัน)
- **โปรเจกต์ Solar SaaS อยู่คนละ repo** (`14-Pi/Solar SaaS/proinventive-solar`) และคนละบัญชี GitHub
  (`enterprice0945`) ไม่เกี่ยวกับงานนี้ ให้แยกเซสชันไว้ต่างหาก
- แก้ไฟล์ที่รากแล้ว **ต้องรัน `sync-docs.cmd`** ก่อน commit ไม่งั้นเว็บจริงจะยังเป็นของเก่า

---

## การตัดสินใจสำคัญ · 20 ส.ค. 2569

**ยกเลิกการส่งคำเชิญ 38 ฉบับรวดเดียว** เปลี่ยนเป็น **ผู้ใช้ร้องขอเข้ามาเอง**

| | แบบเดิม (ยกเลิกแล้ว) | แบบใหม่ (ใช้จริง) |
|---|---|---|
| จุดเริ่ม | ยิงอีเมล 38 ฉบับพร้อมกัน | ผู้ใช้ติดต่อขอใช้งานเข้ามาเอง |
| ใครกดปุ่ม | ผู้ดูแลระบบ | ผู้ใช้เอง |
| ความเสี่ยง | 38 คนได้จดหมายที่ไม่ได้ขอ ตอบคำถามไม่ทัน | ไม่มี ทีละคนตามจังหวะจริง |
| รหัสผ่าน | ระบบบังคับให้ตั้งใหม่ | ผู้ใช้ตั้งเองตั้งแต่แรก |

สคริปต์ `invite-users.mjs` **ยังเก็บไว้** ใช้ตรวจสถานะด้วย `--check` และเผื่อวันหลังอยากส่งเป็นชุด

---

## Flow ที่ใช้จริง

```
ผู้ใช้ติดต่อขอใช้งาน
        │
        ▼
ผู้ดูแลเช็คว่าอีเมลอยู่ในรายชื่อที่อนุญาตหรือยัง
        │
        ├── มีอยู่แล้ว ──────────────┐
        │                            │
        └── ยังไม่มี                  │
             │                       │
             ▼                       │
   Settings → การ์ด                  │
   "รายชื่อผู้มีสิทธิ์ใช้งาน"          │
   → พิมพ์อีเมล → เพิ่มรายชื่อ        │
             │                       │
             └───────────┬───────────┘
                         ▼
        บอกผู้ใช้ให้เข้าเว็บ กด "สมัครใช้งาน"
                         ▼
        กรอกอีเมล ชื่อ และตั้งรหัสผ่านเอง
        (ระบบตรวจอีเมลให้ทันทีขณะพิมพ์)
                         ▼
              ระบบส่งอีเมลยืนยัน
                         ▼
        ผู้ใช้กดลิงก์ในอีเมล → เข้าใช้งานได้เลย
```

**ลืมรหัสผ่าน** → หน้าเข้าสู่ระบบมีลิงก์ *ขอลิงก์ตั้งรหัสใหม่ทางอีเมล* ผู้ใช้ทำเองได้ ไม่ต้องผ่านผู้ดูแล

---

## ตรวจสอบแล้ว · 20 ส.ค. 2569

| สิ่งที่ตรวจ | วิธีตรวจ | ผล |
|---|---|---|
| 38 อีเมลจาก `user.js` อยู่ในตาราง `allowed_emails` | ถามฐานข้อมูลตรง | ✓ ครบ (ในตารางมี 41 รายการ) |
| อีเมลที่อนุมัติแล้ว → ระบบบอกว่าสมัครได้ | กรอกในฟอร์มจริงบนเว็บที่เผยแพร่ | ✓ `✓ อีเมลนี้ได้รับอนุมัติแล้ว สมัครได้เลย` |
| อีเมลนอกรายชื่อ → ระบบบอกก่อนกดสมัคร | กรอกในฟอร์มจริง | ✓ `✗ อีเมลนี้ยังไม่ได้รับอนุมัติ` |
| คนนอกอ่านรายชื่อผู้มีสิทธิ์ทั้งหมดได้ไหม | เรียก API ด้วยคีย์ anon | ✓ อ่านไม่ได้ ได้ `[]` — RLS กันอยู่ |
| ทริกเกอร์กันคนนอกที่ชั้นฐานข้อมูล | สั่งสร้างบัญชีนอกรายชื่อด้วยคีย์ service_role | ✓ ถูกปฏิเสธ `Database error saving new user` |
| ระบบส่งอีเมลออกจริง | ส่งจริง 1 ฉบับหาบัญชีทดสอบ | ✓ `ส่งคำเชิญแล้ว` |
| ยังไม่มีลูกค้าคนไหนถูกเชิญโดยไม่ตั้งใจ | `--check` | ✓ 38 คนยังไม่ถูกเชิญ ไม่มีอีเมลหลุดออกไป |
| ลบบัญชีทดสอบแล้วของจริงไม่กระทบ | นับบัญชี โปรไฟล์ โครงการ รายชื่ออนุญาต | ✓ เหลือบัญชี 1 โครงการ 1 รายชื่อ 41 ครบเท่าเดิม |

---

## ทำเสร็จแล้ว

| หัวข้อ | สถานะ |
|---|---|
| ย้ายฐานข้อมูลขึ้น Supabase + ระบบแชร์โครงการ | ✓ |
| โปรเจกต์จริง `rcaslesvgilknupqmwrp` เชื่อมแล้ว | ✓ |
| `supabase-config.js` บนเว็บจริงชี้โปรเจกต์ใหม่ | ✓ |
| ตาราง `profiles` `projects` `project_members` `project_list` `allowed_emails` + bucket รูป | ✓ |
| ทริกเกอร์ `enforce_invite_only` · `on_auth_user_created` | ✓ |
| ตั้งค่า SMTP + Redirect URLs | ✓ |
| ฟอร์มสมัครใช้งานเอง + ตรวจอีเมลทันทีขณะพิมพ์ | ✓ |
| ลิงก์ขอตั้งรหัสผ่านใหม่ทางอีเมล | ✓ |
| การ์ดจัดการรายชื่อผู้มีสิทธิ์ในหน้า Settings | ✓ |
| ปุ่มเปลี่ยนรหัสผ่าน / ชื่อที่แสดง | ✓ |
| `invite-users.mjs` + `--check` `--dry-run` `--only` + อ่านคีย์จาก `.env` | ✓ |
| จัดโฟลเดอร์ ย้ายไฟล์ที่ไม่ใช้แล้วไป `bin/` | ✓ |
| ลบบัญชีทดสอบ `+asctest` `+asctest2` | ✓ 20 ส.ค. เหลือบัญชีจริงบัญชีเดียว |

---

## ค้างอยู่

### 1. สามบัญชีที่ยังไม่ใช่อีเมล

`supet` `lpee` `pat` ใน `user.js` ยังเป็นชื่อผู้ใช้ ไม่ใช่อีเมล
ถ้าคนเหล่านี้จะเข้าระบบ ต้องเพิ่มอีเมลจริงเข้าตาราง `allowed_emails` ผ่านการ์ดในหน้า Settings

### 2. ผู้ดูแลระบบไม่ถูกตั้งอัตโนมัติ

`user.js` ตั้ง `ADMIN_USERS = [lpee]` แต่ `lpee` ไม่ใช่อีเมล จึงไม่มีใครถูกตั้ง `is_admin` อัตโนมัติ
ถ้าจะเพิ่มผู้ดูแลคนใหม่ ต้องสั่งใน SQL Editor

```sql
update public.profiles set is_admin = true where email = 'อีเมลของเขา';
```

### 3. บอกผู้ใช้ว่าระบบพร้อมแล้ว

ทั้ง 38 คนอยู่ในรายชื่อที่อนุญาตแล้ว เข้าเว็บกด **สมัครใช้งาน** ได้ทันที ไม่ต้องรอใครเชิญ

### 4. ให้ผู้ใช้เดิมย้ายข้อมูลขึ้นคลาวด์

แต่ละคนทำเองที่เครื่องของตัวเอง (ข้อมูลเดิมแยกกันอยู่ในเบราว์เซอร์แต่ละเครื่อง)
Settings → การ์ด *ย้ายโครงการเดิมขึ้นคลาวด์* → **ส่งขึ้นคลาวด์ทั้งหมด** — กดครั้งเดียวต่อเครื่อง

---

## ข้อควรระวังที่ยังอยู่

- ไม่มีการล็อกเมื่อแก้พร้อมกัน คนบันทึกทีหลังทับคนแรก
- ต้องออนไลน์เสมอ ไม่มีโหมดออฟไลน์
- ถอดชื่อออกจาก `allowed_emails` = กันสมัครใหม่เท่านั้น ไม่ได้ลบบัญชีที่มีอยู่แล้ว
- `docs/mobi.html` `docs/manifest.json` `docs/sw.js` ไม่มีต้นฉบับที่ราก ต้องแก้ใน `docs/` โดยตรง

## คำสั่งที่ใช้บ่อย

```
node supabase/invite-users.mjs --check    # ดูว่าใครสมัครแล้ว ใครยัง
sync-docs.cmd                             # คัดลอกไฟล์ที่รากเข้า docs ก่อน commit
```

รายละเอียดการติดตั้งทั้งหมดอยู่ใน [`supabase/README.md`](supabase/README.md)
